apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
  namespace: nginx-ingress
spec:
  rules:
    - host: kuard.investbit.eu
      http:
        paths:
          - path: /
            backend:
              serviceName: kuard
              servicePort: 80
